[package]
name = "syscalls"
version = "0.0.0"

[lib]
path = "lib.rs"

[dependencies]
stack_dst = { path = "../../../externals/crates.io/stack_dst", default-features = false }
kernel = { path = "../../Core" }
gui = { path = "../gui" }

